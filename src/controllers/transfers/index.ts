import { createTransfer } from './transfer.create'
import { resultsTransfer } from './transfer.results'
import { resultTransfer } from './transfer.result'
import { deleteTransfer } from './transfer.delete'
import { updateTransfer } from './transfer.update'

export const transferController = {
	createTransfer,
	resultsTransfer,
	resultTransfer,
	deleteTransfer,
	updateTransfer
}
